name: Inject API Key and Deploy

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    env:
      GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify secret present (redacted in logs)
        run: test -n "${GEMINI_KEY}"

      - name: Inject GEMINI_KEY into index.html
        run: sed -i "s|__API_KEY__|${GEMINI_KEY}|g" index.html

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload static files
        uses: actions/upload-pages-artifact@v2
        with:
          path: ./

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v3
        id: deployment
